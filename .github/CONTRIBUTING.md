# Contributing

Open an issue before proposing a behavior change. Pull requests should remain within the documented pipeline scope, update relevant documentation, and pass pre-commit plus the nf-test regression suite.

Vendored files under `modules/nf-core` must be managed with nf-core tooling and kept synchronized with `modules.json`. Pipeline-specific changes belong under `modules/local` with their upstream provenance documented.
