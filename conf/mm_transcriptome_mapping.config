/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run nf-core/rnaseqpipeline -profile test,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

// process {
//     resourceLimits = [
//         cpus: 4,
//         memory: '24.GB',
//         time: '1.h'
//     ]
// }

params {
    config_profile_name        = 'Test profile'
    config_profile_description = 'Minimal test dataset to check pipeline function'

    // Input data - using local test samplesheet
    input = 'samplesheet_AT_mm_a3.csv'
    
    // Output directory
    outdir = './results_mm_a3_transcriptome/'
    
    // Genome references
    genome = 'GRCm38'
    igenomes_base = 's3://ngi-igenomes/igenomes'
    aligner = 'bowtie2'
    igv_gff = 'mm_samples/GCF_000001635.27_GRCm39_genomic.gff.gz'
    igv_sites = 'mm_samples/GSE17051_cLAD_regions_ensembl.bed'
    transcriptome = 'mm_samples/GCF_000001635.27_GRCm39_rna_from_genomic.fna.gz'

    
    // FastQC options
    fastqc_threads = '32'
    
    // TrimGalore options
    trimgalore_min_length = '20'
    trimgalore_min_qual = '20'
    
    // STAR alignment options
    readFilesCommand = 'zcat'
    outSAMtype = 'BAM SortedByCoordinate'
    
    // Picard options
    picard_prefix = 'picard'

    // MERGESTRINGTIE options
    genetable_outfile = 'gene_table_TPM'
    
    // Reference genome configuration
    igenomes_reference = 'GRCm38'
}
